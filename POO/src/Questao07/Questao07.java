package Questao07;

public class Questao07 {
  /** Herança é um mecanismo que permite criar novas classes a partir de classes já existentes, aproveitando-se das características
   * existentes na classe a ser estendida.De modo simplificado herança é "uma classe(classe filha) que tem os mesmos atributos de outra(classe mãe),
   *  mais alguns atributos distintos. Já a composição, acontece quando uma determinada
   *  classe A está contida em outra determinada classe B.
   *  Portanto,a diferença entre herança e composição é bem simples e pode ser resolvida com
   *  apenas algumas perguntas simples. Para herança a pergunta é" Classe A é uma classe B?" e para composição a pergunta é 
   *  "A classe A está contida na classe B?".
   * 
   */
}
