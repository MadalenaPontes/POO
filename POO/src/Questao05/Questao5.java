package Questao05;

public class Questao5 {
	/** A sobreposição(sobrescrita) nos permite reescrever nas classes filhas, métodos criados
	 *  inicialmente na classe pai, os métodos que serão sobrepostos. 
	 *  Devem possuir o mesmo nome, tipo de retorno e quantidade de parâmetros de método inicial, 
	 *  porém o mesmo será implementado com especificações da classe atual. 
	 *  Já a sobrecarga de métodos consiste basicamente na criação de dois ou mais
	 *  métodos com nomes totalmente iguais em uma classe, contanto que suas listas de argumentos 
	 *  sejam diferentes para que seja feita a separação dos mesmos.
	 * 
	 */

}
