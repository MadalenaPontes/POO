package Questao06;

public class Questao6 {
	/** Interface  é um contrato onde quem assina se responsabiliza por implementar esses métodos(cumprir o contrato). 
	 * Ela só expõe o que o objeto deve fazer, e não como ele faz, nem o que ele tem.Como  ele faz vai ser definido 
	 * em uma implementação dessa interface.
	 * 
	 * Em classes abstratas,toda classe concreta que herdar de uma classe abstrata deve fornecer uma implementação
	 *  concreta para os métodos abstratos(caso existam), apesar de não podermos dar new em classes abstratas,
	 *  todas elas também tem um construtor ; elas também não são obrigadas a ter um método abstrato, as classes abstratas podem ser herdadas por 
	 * outras classes abstratas; 
	 * 
	 * Usamos Interface quando precisamos que classes de diferentes origens
	 * (herança) tenham métodos em comum e possam ser acessados através de uma mesma variável.
	 * A classe abstrata é utilizada quando você quer que uma classe que herde dela
 	 *implemente alguns métodos, mas que também possam ter métodos implementados na própria classe abstrata.
	 * 
	 
	 */ 
	
	
	

}
